# Config for multi-device full finetuning in full_finetune_distributed.py
# using a Qwen2.5 0.5B model
#
# This config assumes that you've run the following command before launching:
#   tune download Qwen/Qwen2.5-0.5B-Instruct
#
# To launch on 2 devices, run the following command from root:
#   tune run --nproc_per_node 2 full_finetune_distributed --config qwen2_5/0.5B_full
#
# You can add specific overrides through the command line. For example
# to override the checkpointer directory while launching training:
#   tune run --nproc_per_node 2 full_finetune_distributed --config qwen2_5/0.5B_full checkpointer.checkpoint_dir=<YOUR_CHECKPOINT_DIR>
#
# This config is for fine-tuning on 2+ GPUs.

output_dir: _ # /tmp may be deleted by your system. Change it to your preference.

# Model arguments
model:
  _component_: torchtune.models.qwen2_5.qwen2_5_0_5b

# Tokenizer
tokenizer:
  _component_: torchtune.models.qwen2_5.qwen2_5_tokenizer
  path: _/vocab.json
  merges_file: _/merges.txt
  max_seq_len: 896

# Checkpointer
checkpointer:
  _component_: torchtune.training.FullModelHFCheckpointer
  checkpoint_dir: _/Qwen2.5-0.5B-subword-Instruct
  checkpoint_files: [
    model.safetensors
    ]
  recipe_checkpoint: null
  output_dir: _/qwen_0.5B_finetuned
  model_type: QWEN2
resume_from_checkpoint: False

# Dataset
dataset:
  _component_: torchtune.datasets.alpaca_cleaned_dataset
  source: json
  split: train
  data_files: _/*.json
  template: torchtune.data.AlpacaInstructTemplate
  column_map:
    instruction: instruction
    input: input
    output: output
  max_seq_len: 896
  packed: True
seed: null
shuffle: False

# Fine-tuning arguments
epochs: 10
max_steps_per_epoch: null
save_per_step: 5000
batch_size: 16
gradient_accumulation_steps: 1  # Use to increase effective batch size
optimizer:
  _component_: torch.optim.AdamW
  fused: True
  lr: 2e-5
optimizer_in_bwd: False  # True saves memory. Requires gradient_accumulation_steps=1
loss:
  _component_: torchtune.modules.loss.CEWithChunkedOutputLoss

# Training env
device: cuda

# Memory management / performance
enable_activation_checkpointing: False  # True reduces memory
enable_activation_offloading: False  # True reduces memory
dtype: bf16
compile: False  # torch.compile the model + loss, True increases speed + decreases memory

# Logging
# metric_logger:
#   _component_: torchtune.training.metric_logging.DiskLogger
#   log_dir: /raid/adal_abilbekov/workspace/torchtune_new_era/logs
# log_every_n_steps: 1
# log_peak_memory_stats: False

metric_logger:
  _component_: torchtune.training.metric_logging.WandBLogger
  # the W&B project to log to
  project: _
  name: _
  log_dir: _/logs
# output_dir: /raid/adal_abilbekov/workspace/torchtune_new_era/logs
log_every_n_steps: 1
log_peak_memory_stats: False

# Profiler (disabled)
profiler:
  _component_: torchtune.training.setup_torch_profiler
  enabled: False

  #Output directory of trace artifacts
  output_dir: _/profiling_outputs

  #`torch.profiler.ProfilerActivity` types to trace
  cpu: True
  cuda: True

  #trace options passed to `torch.profiler.profile`
  profile_memory: False
  with_stack: False
  record_shapes: True
  with_flops: False

  # `torch.profiler.schedule` options:
  # wait_steps -> wait, warmup_steps -> warmup, active_steps -> active, num_cycles -> repeat
  wait_steps: 5
  warmup_steps: 3
  active_steps: 2
  num_cycles: 1

sub_length: 100000
train_type: instruct